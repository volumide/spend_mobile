import { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "app.netlify.partyspray",
  appName: "party spending",
  webDir: "dist",
  bundledWebRuntime: false
}

export default config

// https://partyspray.netlify.app/
